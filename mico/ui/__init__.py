"""User-facing surfaces (CLI, web). No business logic lives here.

Per docs/mico-implementation-design.md: mico/ui must never import
mico.brain directly (enforced by tools/check_layers.py in CI).
"""
